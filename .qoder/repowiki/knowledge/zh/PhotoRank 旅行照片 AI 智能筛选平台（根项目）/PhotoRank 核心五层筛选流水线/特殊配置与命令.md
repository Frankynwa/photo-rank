运行入口为 `python core/pipeline.py`；Layer3 需要 RTX 5070 及 CUDA 环境以加载 NIMA-VGG16 模型；Layer5 需配置 `.env` 中的 `QWEN_API_KEY`（硬编码路径为 `C:\Users\Administrator\AppData\Local\hermes\.env`），且需设置 `QWEN_BASE_URL`、`QWEN_MODEL`、`PROXY` 等环境变量。