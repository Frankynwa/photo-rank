本模块为纯文档仓库，按用途分为两个子目录：`wiki/` 存放面向用户的结构化 Wiki（Home.md 作为入口索引，Quick-Start.md 提供快速上手，Architecture.md 以 ASCII 架构图描述 Web UI → Pipeline（5 层流水线）→ Output 的分层架构），`research/` 存放独立的研究 Markdown 报告（审美模型、Qwen-VL API、Web UI 框架选型等）。文档之间通过相对路径互相引用（如 Home.md 指向 `../research/*.md`），形成“入口索引 + 专题报告”的扁平结构，无代码依赖关系。